package com.example.hysacam.Repository;

import com.example.hysacam.Entity.Ville.StatusPassage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatusPassageRepository extends JpaRepository<StatusPassage,Integer> {
}
